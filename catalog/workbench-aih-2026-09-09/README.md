# AIH first-party Catalog successor

This sequence-3 head retains all 428 sequence-2 members and adds nine exact
Core 0.6.0 profiles: three skill/agent packs and six MCP configurations.
The original sequence-2 expiry remains the ceiling. The existing administrator
signer and predecessor are unchanged.

The profiles bind Scanner publication from Core candidate
`54ceab4118aade25a8a07608532b434feb0a6e6b`, produced by Scanner workflow run
[34336289122](https://github.com/samartomar/aih-scan/actions/runs/34336289122).
The complete verified report, its sealed wrapper, qualification draft, and
license notices are retained under `defaults/workbench/aih/source-reports`.

The GitHub MCP configuration retains its blocked `trust.external-egress`
finding. Every new entry carries its declared scope limitation. Configuration
review does not cover the executable MCP server or grant organization approval.
The usage-metering hook remains in the ten-component report but is not a
Catalog member because hooks are outside the supported qualification grammar.

The inner signature can be verified against the existing genesis public root
and `previous-head.json`. Protected outer publication and Core admission are
separate steps; this source artifact alone does not establish either.
